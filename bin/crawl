#!/usr/bin/env ruby

require "bundler/setup"
Bundler.require

c = Crawler.new
puts "Exploring new config ..."
repos = c.explore
Parallel.each(repos, :in_processes => 8) do |repo|
  repo.clone
end

puts "Done."
